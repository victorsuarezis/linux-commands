Pre-requisitos
--------------
- Python 2.7
- MariaDB
- Redis
- wkhtmltopdf (optional, required for pdf generation)


Leer
----
- https://github.com/frappe/bench
- http://frappe.github.io/erpnext/install.html
- http://redis.io/topics/quickstart


Instalar Utilidades
-------------------
# dnf update -y
# dnf install -y git mercurial gcc make libxml2-devel libxslt-devel python-devel openldap openldap-devel zip unzip zlib-devel python-pillow python-pillow-devel openssl openssl-devel libjpeg-turbo libjpeg-turbo-devel python-virtualenv wkhtmltopdf wkhtmltopdf-devel npm nodejs


Instalar MariaDB
----------------
# dnf install -y mariadb mariadb-server mariadb-devel
# systemctl start mariadb.service
# systemctl enable mariadb.service
# mysql_secure_installation
# nano /etc/my.cnf
[client-server]

[mysqld]
innodb-file-format = barracuda
innodb-file-per-table = 1
innodb-large-prefix = 1
character-set-client-handshake = FALSE
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[mysql]
default-character-set = utf8mb4

!includedir /etc/my.cnf.d

# systemctl restart mariadb.service


Instalar Redis
--------------
# dnf install -y redis
# systemctl start redis.service
# systemctl enable redis.service
# redis-cli ping


Instalar Bench/Frappe/ERPNext
-----------------------------
$ mkdir -p ~/Workspace/frappe && cd ~/Workspace/frappe
$ sudo pip install --upgrade pip
$ git clone https://github.com/frappe/bench bench-repo
$ sudo pip install -e bench-repo
$ bench init frappe-bench
$ cd frappe-bench
$ bench get-app erpnext https://github.com/frappe/erpnext
$ bench new-site site1.local
$ bench install-app erpnext
$ bench start

Ingresar al navegador http://localhost:8000