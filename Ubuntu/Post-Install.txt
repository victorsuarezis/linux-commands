$ sudo apt update
$ sudo apt upgrade
$ sudo apt install vim shutter git texlive-full openjdk-8-jdk docker.io docker-compose build-essential vlc rar unrar meld gdebi xclip gradle plantuml


Generar y copiar clave pública SSH:
$ ssh-keygen
$ cat ~/.ssh/id_rsa.pub | xclip -sel clip


Información de rendimiento tarjeta gráfica:
$ sudo apt install glmark2
$ glmark2


Listar tarjetas de video:
$ sudo lshw -c video


Ver tarjeta en uso:
$ lspci -nnk | grep -i vga -A3


Agregar Java Home:
$ sudo nano /etc/environment
JAVA_HOME="/usr/lib/jvm/java-8-openjdk-amd64"
